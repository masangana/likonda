<div class="container">
	<div class="is-flex flex-grow-1 justify-content-center align-items-center">
		<h1 class="fw-thin"><%= t('Likonda') %></h1>
	</div>
	<div class="is-flex flex-direction-column buttons">

		<% if user_signed_in? %>
			<%= link_to "HOME", groups_path, class: "button is-primary" %>
		<% end %>
        <% unless user_signed_in? %>
            <%= link_to "LOG IN", new_user_session_path, class: "button is-primary" %>
            <%= link_to "SIGN UP", new_user_registration_path, class: "button" %>
        <% end %>
	</div>
</div>